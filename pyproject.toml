[project]
name = "donutnodes"
description = "Block-weighted LoRA stacking with CivitAI metadata, hash resolution, and browser integration; Donut Detailer for per-block model tuning; TeaCache acceleration; tiled upscaling; CFG sampling with 18 curve types; and reference-based spectral sharpening."
version = "3.0.9"
license = {file = "LICENSE"}

[project.urls]
Repository = "https://github.com/DonutsDelivery/ComfyUI-DonutNodes"
#  Used by Comfy Registry https://comfyregistry.org

[tool.comfy]
PublisherId = "donutsdelivery"
DisplayName = "ComfyUI-DonutNodes"
Icon = ""
includes = ["assets/uncensorfix.f32", "assets/README.md", "model_sources.json", "docs/model-sources.md"]
